[tox]
envlist = py27,py35,lint

[testenv]
deps = -r{toxinidir}/requirements.txt
commands =
    nosetests -svx tokenlib/tests --with-coverage --cover-package=tokenlib

[testenv:lint]
commands =
    pylint --rcfile pylintrc --disable=W0511 --ignore tests tokenlib

